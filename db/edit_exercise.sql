update exercises
set sets = ${sets}
where exercise_id = ${exercise_id};

update exercises
set reps = ${reps}
where exercise_id = ${exercise_id};

update exercises
set weight = ${weight}
where exercise_id = ${exercise_id};